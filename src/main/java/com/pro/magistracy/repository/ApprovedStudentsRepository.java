package com.pro.magistracy.repository;

import com.pro.magistracy.model.ApprovedStudents;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ApprovedStudentsRepository extends JpaRepository<ApprovedStudents, Long> {
}
